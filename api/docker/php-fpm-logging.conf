; Adds request duration to php-fpm's access log (default format has no
; timing field at all). Loaded after docker.conf (alphabetical include
; order) so it's the one that sticks for the [www] pool.
[www]
access.format = "%R - %u %t \"%m %r\" %s %{milli}dms"
