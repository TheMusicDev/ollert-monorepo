# Planning Log

## 2026-08-19
* **Initialization**: Created the planning OKF bundle for Ollert (stripped-down Trello clone: CakePHP backend, Supabase Auth, Vite+React frontend).
* **Decisions**: MVP scope set to bare-bones (boards/lists/cards/drag-drop/board members, no roles). App data placed in CakePHP-owned MySQL, not Supabase Postgres — Supabase used auth-only. JWT verification set to JWKS/RS256 with HS256 documented as fallback. Repo layout set to monorepo (`/api`, `/web`, `/planning`).
* **Realtime reconsidered**: Initially requested for day 1, but Supabase Realtime requires the data to live in Supabase's own Postgres (it watches Postgres logical replication), which conflicts with the MySQL/CakePHP-owned-DB decision. Options were a self-hosted Pusher-protocol relay (Soketi) or SSE from CakePHP; user chose to drop realtime from MVP entirely rather than take on either now. Revisit in a later phase — see [Roadmap](roadmap.md).
* **Update**: Added `organizations` above boards — an org has many boards. Dropped `board_members`; added `org_members` instead. Access is org-scoped: any org member has access to every board under that org (no per-board membership/roles in v1). Updated [Data Model](data-model.md), [API Contract](api-contract.md), and [Roadmap](roadmap.md) accordingly.
* **Update**: Resolved the review items from `todos.md`. Added `max_orgs`/`max_boards_per_org` quota columns to `users` (defaults 1/3, app-enforced); board creation is now org-owner-only. JIT provisioning confirmed as find-or-create per request. CORS set to an origin allow-list with no credentials mode (Bearer-token auth, no cookies). Standard `{ error: { message, code, fields? } }` response envelope defined. Deployment: shared PHP host over SSH, local (non-CI) scripts split by package, writing them deferred to Phase 3. Testing stack set: PHPUnit / Vitest / Playwright (e2e). `cakephp/migrations` confirmed as the migrations tool. FE router set to TanStack Router as a plain client-side SPA — TanStack Start considered and rejected, since there's no Node server to run its SSR/server-functions model and forcing a "no SSR" mode fights the framework. Updated [Architecture](architecture.md), [Data Model](data-model.md), [API Contract](api-contract.md), and [Roadmap](roadmap.md) accordingly.

## 2026-08-19 (cont.)
* **Primary keys → UUID**: All entity PKs (and referencing FKs) switched from auto-increment int to UUID (`char(36)`, migrations `uuid` type). CakePHP's ORM auto-generates a UUID on save when the PK column type is `uuid`, so this needs no extra plugin/hook. `users.supabase_uid` remains a distinct UUID column from the new `users.id`.
* **FE framework reversed → TanStack Start**: Prior decision (this same day) picked plain TanStack Router specifically to avoid TanStack Start, since Start's SSR/server-functions model needs a Node server and the deploy target (shared PHP host) has none. User now wants Start regardless. Resolved by running Start in **SPA mode** — SSR and server functions disabled, static client build, same deploy shape (build + sync to PHP host) as the plain-Router plan it replaces. If the deploy target ever gains a Node runtime, SSR can be turned on.
* **FE toolchain**: Bun adopted as package manager/runtime for `/web` (replaces npm implicitly). Tailwind CSS and Base UI (headless component primitives) added for styling/components — no prior decision existed for either.
* Updated [Architecture](architecture.md), [Data Model](data-model.md), and [Roadmap](roadmap.md) accordingly. [API Contract](api-contract.md) unaffected (int vs. uuid IDs don't change endpoint shapes, just the ID string format in URLs/payloads).

## 2026-08-19 (cont. 2)
* **Timestamps/soft delete convention**: every table gets `created`/`modified` (core CakePHP `TimestampBehavior`, replacing the ad hoc `created_at`/`updated_at` names used in the first draft) and a `deleted` datetime via the `muffin/trash` plugin. All `DELETE /api/...` endpoints are now soft deletes.
* **List/card quotas added**: `max_lists_per_board` (default 5) and `max_cards_per_board` (default 100) added to `users`, checked against the org owner regardless of who's creating, same pattern as `max_orgs`/`max_boards_per_org`. Closes the previously-flagged gap where list/card counts were unbounded.
* Updated [Architecture](architecture.md), [Data Model](data-model.md), [API Contract](api-contract.md), and [Roadmap](roadmap.md) accordingly.
* **Remaining gaps**: everything else identified in the gap review (404 handling, pagination, API versioning, Supabase project/env vars, JWKS TTL, local dev story, PHP/Bun version pins, FE 401 handling, auth redirect URLs, visual design starting point, CI, fixture/seed strategy, field length/uniqueness rules, empty-list first position value) written up with options + recommendations in `gaps-found.md` (repo root) for review — not yet resolved.

## 2026-08-19 (cont. 3)
* **Pagination**: added to every top-level collection endpoint (`GET /api/orgs`, org members, org boards) — `?page=`/`?limit=` (default 20, clamp to 100), `{ data, meta }` envelope. Driven by the list/card quotas being per-user columns, not a fixed ceiling, so raised quotas could make these collections large. `GET /api/boards/:id`'s nested lists/cards stay unpaginated — a kanban board needs its full state to render/drag-drop, and `max_lists_per_board`/`max_cards_per_board` already bound that payload.
* **Root `.env.example` added**: documents every env var across `docker/`, `/api`, `/web` in one place (neither package is scaffolded yet). Supabase project itself not yet created — values are placeholders until then.
* **Docker dir reviewed**: user had already created `docker/docker-compose.yml` (MariaDB 11.8 + Mailpit) plus `docker/.env`/`docker/.env.example`. Confirmed workable. Found `docker/.env` and `docker/data/` weren't gitignored — fixed. Mailpit's purpose flagged as a placeholder (Supabase hosted auth emails bypass it; useful once CakePHP sends its own email). Documented in [Architecture](architecture.md#local-development).
* **PHP pinned to 8.5**, Bun pinned to **v1.3.14** (via `package.json`'s `packageManager` field, not a `.bun-version` file — not a real Bun convention).
* **Design starting point**: color palette, typography (Inter), shadow token, and layout pattern (sidebar/navbar/cards/tables) extracted from `windmill-dashboard-react`/`windmill-react-ui` (MIT) at the user's request — explicitly the tokens/pattern, not the `@windmill/react-ui` component library itself (unmaintained, Tailwind v1-era, would conflict with the Base UI choice). New concept doc **[Design](design.md)** added to the OKF bundle and linked from `index.md`.
* **Remaining gap-review items resolved** (user agreed with all standing recommendations): 404s use the standard error envelope (`code: "not_found"`); API versioning skipped (single consumer); JWKS TTL set to 15 min; FE treats 401 as unrecoverable (redirect to login, no retry-after-refresh); FE gains an `/auth/callback` route for Supabase redirects; no CI for the MVP; PHPUnit fixtures / ad hoc Vitest mocks / API-seeded Playwright for test data; `name`/`title` fields `varchar(255)` required no-uniqueness; empty-list first `position` bootstraps to `1.0`.
* Updated [Architecture](architecture.md), [Data Model](data-model.md), [API Contract](api-contract.md), [Roadmap](roadmap.md); added [Design](design.md). `gaps-found.md` (repo root) rewritten to show every item resolved, following the same pattern as `todos.md`.

## 2026-08-19 (cont. 4)
* **`be-tasks.md`/`fe-tasks.md` added** (repo root): full backend/frontend implementation task lists derived from the bundle, each split into a Section 1 foundation branch (shared infra, merged first) and Section 2 independently-delegable branches (one per resource/page, each owning its own files to avoid merge collisions).
* **Root `.env.example` retired, split into `.env.be.example`/`.env.fe.example`** (repo root): the combined file no longer matched the be-tasks/fe-tasks split. `.env.be.example` also corrected to CakePHP's actual default env var names (`APP_NAME`, `DEBUG`, `APP_ENCODING` — not the `APP_ENV`/`APP_DEBUG` the combined file had guessed). `docker/.env.example` unaffected (docker-only vars, e.g. `DB_ROOT_PASSWORD`/`MAILPIT_*`, stay there). `.gitignore` updated for `.env.be`/`.env.fe`.

## 2026-08-20

* **`.env.be.example` retired, superseded by `api/.env.example`**: now that `/api` is scaffolded, the root-level placeholder moved to live next to what it configures. Merged in the stock CakePHP skeleton's `api/config/.env.example` (added `APP_FULL_BASE_URL`, optional `DATABASE_TEST_URL`/`CACHE_*_URL`/`LOG_*_URL`/`EMAIL_TRANSPORT_DEFAULT_URL` DSN overrides), then deleted the stock file. `DB_HOST`/`DB_PORT`/`DB_DATABASE`/`DB_USERNAME`/`DB_PASSWORD` replaced by a single `DATABASE_URL` DSN. `.gitignore` updated (root: `.env.be` entry dropped; `api/.gitignore`: `/config/.env` → `/.env`).
* **`config/app_local.php` removed from the CakePHP scaffold**: no longer generated by `composer install`, no longer loaded by `bootstrap.php`. All per-environment config now flows through `.env` (loaded from `api/.env`, not the stock `api/config/.env`) into `app.php` directly — one config file instead of two. Full rationale and the DSN-over-discrete-vars convention this reinforces written up in `api/CakePHPDivergance.md` (new).

## 2026-08-21

* **Open gap: org-ownership check has no server-computed field.** `api-contract.md`'s org resource only documents `owner_id` (a local `users.id`), but the FE only ever holds the Supabase identity (`session.user.id`) — there's no `/me` endpoint or JWT-embedded local id to resolve "is the current user this org's owner" client-side. Surfaced independently across the `feat/web-orgs`, `feat/web-org-members`, and `feat/web-boards` Section 2 branches, each shipping a different stopgap (an optional `is_owner` field assumed present on the org response with fail-safe UI if absent, vs. matching the session email against loaded member rows). **Not yet resolved** — needs a real decision (most likely: `GET /api/orgs/:id` and `GET /api/orgs` returning a server-computed `is_owner: boolean`) when `feat/api-organizations` is built, then the three FE branches reconciled to match whatever ships.

* **`is_owner` gap resolved (contract + helper only)**: `feat/api-shared-helpers` settled the shape predicted above — `GET /api/orgs`/`GET /api/orgs/:id` will return a server-computed `is_owner: boolean` on each org resource, documented in [API Contract](api-contract.md#organizations). Also resolved `data-model.md#org_members`'s open "is the owner an explicit member row" question: `App\Service\OrgAuthorizationService::isOrgMember()`/`isOrgOwner()` treat the org owner as a member via `organizations.owner_id` regardless of whether an `org_members` row also exists for them, so `feat/api-organizations` is free to pick either convention when creating an org without affecting authorization checks. The field itself is still only *emitted* once `feat/api-organizations` implements `index`/`view` — this branch only shipped the contract doc and the helper it depends on (`isOrgOwner`). The three FE branches' stopgaps still need reconciling once that lands.

* **`is_owner` gap fully closed**: `feat/api-organizations` shipped `index`/`view` actually emitting `is_owner` (`OrganizationsController::serializeOrg()`), and confirmed the resource body is snake_case throughout (`$org->toArray()` plus the appended field — only the pagination `meta` envelope uses camelCase). The three FE branches were reconciled against the real shape on `fix/cors-port-owner-reconciliation`: `feat/web-orgs`/`feat/web-boards` had guessed the field name/type correctly and just needed the optionality dropped; `feat/web-org-members` had guessed camelCase field names and used a client-side email-matching mechanism instead of a real field — rewritten to the real snake_case shape (including the nested `user` object the `OrgMembers belongsTo Users` association actually returns) and to read `org.is_owner` directly.

## 2026-08-22

* **Sidebar's "dark background" reversed**: [Design](design.md#layout-pattern) originally called for the left sidebar to have a permanently dark background, independent of the light/dark toggle (a deliberate accent, matching apps like Trello's own dark nav rail). In practice this read as a bug — toggling the theme visibly changed the navbar and content area but appeared to do nothing to the sidebar — flagged during a UI bug pass on `feat/ui-improvements`. Confirmed as unintentional and reversed: the sidebar is now themed the same as the rest of the shell (`Sidebar.tsx`), and [Design](design.md#layout-pattern) updated to drop the dark-background mandate.

## 2026-08-23

* **MCP server designed (not yet built)**: decided to expose Ollert's REST API as MCP tools for claude.ai's hosted custom-connector feature, via a new Node/TypeScript service (`mcp/`, Bun + Hono + official `@modelcontextprotocol/sdk`), sibling to `api/`/`web/`. Rejected building it in PHP — both PHP MCP options (`mcp/sdk`, CakeDC's `cakephp-mcp`) are pre-1.0/immature. `mcp/` is a thin client of the existing API only — no reimplemented business logic/authorization/quotas. Auth: claude.ai's hosted connectors require OAuth 2.1 (no manual-token option), and Supabase Auth's new native OAuth 2.1/OIDC server (beta since Nov 2025) fills that role — it issues tokens signed with the same JWKS `api/`'s `AuthMiddleware` already verifies, so `mcp/` is a pure Resource Server (validates Bearer tokens, publishes RFC 9728 metadata pointing at Supabase's AS) and never issues/stores tokens itself. One new surface required: `web/` needs a consent-screen route, since Supabase's OAuth flow doesn't host one. Deploy target: Kamal to a local-network machine, exposed via Cloudflare Tunnel — the shared PHP host has no Node runtime. Full design, open risks (`aud`-claim compatibility with OAuth-issued tokens chief among them, unverified), and implementation order written up in new concept doc **[MCP Server](mcp-server.md)**, added to the OKF bundle and linked from `index.md`. Implementation not started — user is setting up the Kamal/Cloudflare deploy target next.

## 2026-08-24

* **MCP server built, typechecks, boots, all routes smoke-tested.** The `mcp/` scaffold was rewritten to the installed SDK's real API (the 2026-08-23 design doc's "Hono + `@modelcontextprotocol/sdk`" was a guess that didn't survive contact with the actual package): `McpServer.registerTool(name, {description, inputSchema: z.object({...})}, cb)` — there is no `.tool()`; `createMcpHandler(factory, {legacy:"stateless"})` returns a web-standard `{fetch}` handler served directly by `Bun.serve` — **Hono dropped entirely** (no `@modelcontextprotocol/hono`, no `hono`, no node-server adapter). The SDK's own auth helpers replace the three hand-rolled auth files: `requireBearerAuth` (fetch-gate → `AuthInfo` or 401 challenge), `oauthMetadataResponse` (serves both `/.well-known` routes, path-aware so `/mcp` → `/.well-known/oauth-protected-resource/mcp`, with CORS/preflight), `buildOAuthProtectedResourceMetadata`, `hostHeaderValidationResponse`/`localhostAllowedHostnames` (DNS-rebinding guard). `verifyToken.ts` now implements the SDK's `OAuthTokenVerifier` interface (returns `AuthInfo`, throws `OAuthError(InvalidToken)`) — still ports `AuthMiddleware.php`'s iss/aud/sub/exp checks 1:1 (ponytail: security parity preserved). zod bumped 3→4 because the SDK's `inputSchema` type requires a `~standard` `jsonSchema` converter zod 3 lacks. All 18 tools registered across `orgs`/`orgMembers`/`boards`/`lists`/`cards`. `bun run typecheck` green. Smoke test (stub AS-metadata doc, since the real one 404s — see below): `/health` 200, RFC 9728 metadata 200 with `authorization_servers`→Supabase, `/mcp` no-token → 401 `WWW-Authenticate` challenge, `/mcp` garbage-token → 401 `invalid_token`, wrong-`Host` → 403, CORS preflight 204. [MCP Server](mcp-server.md) updated to reflect what shipped (SDK API, no-Hono, zod 4, implementation steps 1–3 marked done, open risks revised).
* **Supabase OAuth server is `feature_disabled` on project `mlowpaysruhaxowhsuva`** (confirmed: `GET https://mlowpaysruhaxowhsuva.supabase.co/.well-known/oauth-authorization-server/auth/v1` → 404 `{"error_code":"feature_disabled","msg":"OAuth server is disabled"}`). This is the live blocker for the end-to-end claude.ai↔Supabase OAuth dance — NOT a code bug; the mcp/ server boots and serves metadata fine with a stub. Enabling it is a Supabase-dashboard action. Until then: the `web/` consent route, inspector testing with a real token, and real deploy all stay blocked. Recorded as the #1 open risk in [MCP Server](mcp-server.md#open-risks).
* **Supabase OAuth server enabled + consent route built.** User enabled the OAuth 2.1 server in the Supabase dashboard (authorization path `/oauth/consent`, Site URL `https://ollert.2719.fyi`) — the `feature_disabled` blocker above is cleared (`GET …/.well-known/oauth-authorization-server/auth/v1` now 200, `issuer` matches `config.jwtIss`, `offline_access` supported). Registered one OAuth app as a **public client** (`token_endpoint_auth_method: none`, PKCE-only) with redirect URI `https://claude.ai/api/mcp/auth_callback`; DCR left off (claude.ai accepts a pasted pre-registered `client_id` via Advanced settings → skips DCR, no open registration endpoint). Public+`none` chosen specifically because claude.ai strictly validates `token_endpoint_auth_method` (supabase PR #2300 found that bug integrating with claude.ai) — public client can't mismatch a secret channel that doesn't exist. Built `web/src/routes/oauth/consent.tsx` (`/oauth/consent`, a public route — not under `_authenticated/`, since it's reached before a session is guaranteed): reads `authorization_id`, shows an inline sign-in (reusing `AuthCard`/`FormField`/`SubmitButton`/`FormError` + `useAuth().signIn`) when there's no session rather than bouncing to `/login` (so the `authorization_id` isn't lost on a round-trip), calls `supabase.auth.oauth.getAuthorizationDetails` to render client name/logo/URI + requested scopes + redirect_uri, then `approveAuthorization`/`denyAuthorization` with `skipBrowserRedirect:true` and `window.location.href = data.redirect_url` (the SDK's auto-redirect + the docs' `redirect_to` naming are both footguns — issue supabase/supabase#45006; `redirect_url` is the real field). `tsc` + `eslint` + `bun run build` green. [MCP Server](mcp-server.md) implementation step 4 + open risks updated.
* **MCP deployed + claude.ai↔Supabase dance verified end-to-end.** `kamal deploy -c config/deploy.mcp.yml` shipped `mcp/` to negrita behind the shared kamal-proxy as `ollert-mcp.2719.fyi` (cloudflared's existing `*.2719.fyi` wildcard covered the new host — no tunnel change). One deploy fix: `/health` had to move *before* the `hostHeaderValidationResponse` DNS-rebinding guard in `mcp/src/server.ts` — kamal-proxy's healthcheck hits the container by docker-internal hostname, whose `Host` isn't in `allowedHostnames` → 403, same shape as api's nginx-serves-`/health`-statically bypass (the CLAUDE.md "Healthcheck Host-header 400" gotcha, real this time). `/health` returns only `ok`, so exempting it costs nothing. Verify: `/health` 200, `/.well-known/oauth-protected-resource/mcp` → `{resource, authorization_servers:[Supabase AS], resource_name:"Ollert"}`, `/mcp`+garbage → 401 `invalid_token`. The web SPA also needed a redeploy — the first claude.ai connect attempt 404'd on `/oauth/consent` because the live web container was still the pre-`518ae57` SHA `fec7ecaf` that predates the consent route; `cd web && bun run build && kamal deploy -c config/deploy.web.yml` brought it to `518ae57` and the route served 200. User then ran the real connector dance in claude.ai and it connected cleanly — which settles the last open risk: **the `aud` claim on OAuth-flow-issued Supabase tokens matches `SUPABASE_JWT_AUD=authenticated`**, no audience-widening needed in `verifyToken.ts`. [MCP Server](mcp-server.md) open risks + implementation step 6 updated. Nothing left to build for the MCP feature.

## 2026-08-24 (cont.)

* **All-in Supabase decided — reverses the 2026-08-19 MySQL decision.** User decided to move Ollert's app data from CakePHP-owned MySQL to Supabase's hosted Postgres, and adopt Supabase Storage + Realtime alongside the existing Supabase Auth. Major overhaul, **decided but not started** — the app currently runs MySQL on negrita. The spine is the DB migration (schema types, `cakephp/migrations` files, MySQL-specific query syntax, the `docker/` local MariaDB dev setup); riding on it: **Storage** → Supabase Storage (S3-compatible, closes the attachments storage question — supersedes the local-FS / self-hosted RustFS / Cloudflare R2 options), **Realtime** → Supabase Realtime (WAL-based, replaces the deferred Soketi plan — no extra infra), **Search** → PostgreSQL full-text search (`tsvector`/`tsquery` + GIN or GiST indexes) + `pgvector` for semantic similarity (supersedes the MySQL-FULLTEXT / Algolia ideas). **Auth and the MCP server are unchanged** — both already verify Supabase JWTs against the same JWKS. Recorded as a forward-looking concept: new **[Supabase Migration](supabase-migration.md)** doc. Current-state docs ([Architecture](architecture.md), [Data Model](data-model.md)) keep describing MySQL until the migration branch executes the swap, then get rewritten — same pattern as [MCP Server](mcp-server.md) being written pre-build then updated post-build. Local-dev sub-decision resolved: `supabase start` local CLI stack (offline, fully isolated from prod, heaviest local infra ~6 containers) over a separate dev hosted project. The migration needs a real data-migration + cutover plan — there's live team data on negrita, not greenfield.
* **Admin app (#20) pulled earlier.** Reason: no way to tweak per-user limits today — the four quota columns on `users` exist but are never mutated after JIT provisioning. Design: add an `is_admin` boolean column to the existing `users` table (no new table — `users` already shadows the Supabase identity, see [Data Model](data-model.md)); admin mutates the existing per-user quota columns. **No global raise — ever**; per-user overrides via admin is the only mechanism, defaults stay the floor. Admin UI is a route in `web/` (one view: list users, change limits), not a separate Vite app. `is_admin` is **not** stored in the JWT — the API gates admin endpoints by querying `users.is_admin` for the caller's `sub` per request, so promote/demote takes effect on the next request (a JWT-embedded flag would be stale for the token's lifetime; Supabase JWTs do include `app_metadata`, but a JWT-embedded `is_admin` would be stale until reissue — per-request lookup gives immediate revocation). First admin bootstrapped via env (`ADMIN_UUID=<supabase uid>`) the API trusts, then promotes others through the UI. The clean version lands post-DB-migration on Postgres `public.profiles`; a minimal version can ship now on the existing MySQL `users` table with identical column shape so the migration just relocates it — no throwaway. [Data Model](data-model.md) updated with the `is_admin` column; the admin API contract is settled — `PATCH /api/admin/users/:id` patches the four quota fields + `is_admin` (promote/demote), partial PATCH ok, pagination same `?page=`/`?limit=` `{data,meta}` shape (see [API Contract](api-contract.md#admin)).
* **Storage question closed.** Attachments and anything file-ish → Supabase Storage (S3-compatible, switchable provider). Closes the storage question raised during the attachment discussion; unblocks attachments (still needs upload/scan + quotas before shipping). Recorded in [Supabase Migration](supabase-migration.md).
* **Social login: Google + Apple only, deferred.** Not GitHub. Deferred because no provider credentials yet; Supabase handles the wiring once obtained. [Architecture](architecture.md) + [Roadmap](roadmap.md) updated.
* **Quotas: no global raise, ever.** The four quota defaults (`max_orgs=1`, `max_boards_per_org=3`, `max_lists_per_board=5`, `max_cards_per_board=100`) stay as the floor. Per-user overrides via the admin feature above are the only mechanism to raise a limit. Closes the open "raise when / paid tier" question — answered: never globally. [Data Model](data-model.md) + [Roadmap](roadmap.md) updated.
* **MCP follow-ons noted (not started).** Read-gap tools identified — the 18 shipped tools are create/move/delete only; `list_lists`, `list_cards`, `get_card`, `get_list` are missing (an agent can't enumerate). Other follow-ons: `mcp/README.md`, pin `@modelcontextprotocol/server`/`zod`/`jose` to concrete versions, tool annotations (`readOnlyHint`/`destructiveHint`/`idempotentHint`). Skip MCP prompts/resources — no real agent use case yet, speculative. Recorded in [MCP Server](mcp-server.md).
* **FE cleanups + testing gaps noted (not started).** `key={orgId}` on org-scoped routes (kills the cross-org state-leak class patched per-branch three times); `web/src/test/setup.ts` global RTL cleanup (`globals: false` means auto-cleanup never fires); `verifyToken.ts` test (forged/expired/wrong-iss → `OAuthError(InvalidToken)`) + a drift guard vs `AuthMiddleware.php` (identical-claims enforcement, no automated check today). See [Roadmap](roadmap.md).

## 2026-08-26

* **FE cleanups + MCP token-verification test + drift guard landed** (`feat/web-cleanups-testing-gaps`). `key={orgId}` added to the two org-scoped routes (`/orgs/$orgId`, `/orgs/$orgId/members`), forcing a full remount on org change and killing the cross-org state-leak class the three per-branch `prevOrgId`/`orgIdForPage` reset guards had patched symptom-by-symptom (`CLAUDE.md` Learnings 2026-08-21 named `key` as the real fix) — those guards deleted as dead. `web/src/test/setup.ts`'s global `afterEach(cleanup)` was already in place (line 10), so the roadmap's "setup.ts global RTL cleanup" item was stale; the four route test files carrying a now-redundant per-file `afterEach(() => cleanup())` (login/signup/forgot-password/auth-callback) were trimmed to rely on the global. `mcp/src/auth/verifyToken.test.ts` added — the first test file in `mcp/` (`bun test`), covering forged/bad-sig/expired/wrong-iss/wrong-aud/missing-sub/missing-email → `OAuthError(InvalidToken)` (mirrors `api/tests/TestCase/Middleware/AuthMiddlewareTest.php`), plus a **bidirectional drift guard** that parses both `mcp/src/auth/verifyToken.ts` and `api/src/Middleware/AuthMiddleware.php` and asserts each enforces the canonical `{iss,aud,exp,sub,email}` claim set with no extras — locks the 1:1 port so a claim added/dropped on one side without the other is caught here (negative-checked: dropping the `email` guard makes the test fail with "TS dropped email", then reverted). `verifyAccessToken` gained an optional `keys` param (defaults to the production remote JWKS) so tests inject a local public key with no JWKS HTTP — non-breaking, production `verifier` export unchanged. `mcp/package.json` gained a `test` script; new `.github/workflows/ci_mcp.yml` runs tsc + `bun test` on PRs to main. Roadmap corrected: MCP follow-ons (PR #33) + FE cleanups + verifyToken test/drift guard all marked done; e2e (Playwright) stays open. `CLAUDE.md` Learnings 2026-08-20 (setup.ts) and 2026-08-21 (key={orgId}) are resolved by this branch, not contradicted — left as historical record.

## 2026-08-27

* **Supabase migration spine landed locally.** Scope decided with the user up front: local-dev-only for this pass (deploy/negrita cutover deferred separately); CakePHP's phinx migrations stay the sole schema authority (no `supabase/migrations`); local Supabase Auth used too, not just Postgres, so the whole stack is prod-isolated. Installed `supabase` CLI (2.116.0), ran `supabase init` at the repo root, `supabase start` (Postgres on `54322`, API gateway/Auth on `54321`, ~9 containers). Generated an RS256 signing key (`supabase gen signing-key --algorithm RS256` → `supabase/signing_keys.json`, gitignored) and wired it via `config.toml`'s `signing_keys_path` so local GoTrue issues RS256 tokens with a real JWKS endpoint instead of the CLI's HS256 default — keeps local auth on the exact same verification code path as prod (`verifyToken.ts`/`AuthMiddleware.php` unchanged), just pointed at a local JWKS URL. Disabled Supabase's own migration runner (`[db.migrations] enabled = false`) to avoid a second schema-tracking system. Swapped `api/config/app.php`'s connection driver `Mysql::class` → `Postgres::class` (both `default` and `test` datasources) and dropped the MySQL-only config (utf8mb4/`flags`/`init` comments). Updated `api/.env` (`DATABASE_URL` → local Postgres, `SUPABASE_JWKS_URL`/`SUPABASE_JWT_ISS` → local) and `web/.env` (`VITE_SUPABASE_URL`/`VITE_SUPABASE_PUBLISHABLE_KEY` → local); `mcp/.env` left pointed at the hosted project since MCP wasn't exercised this pass. Ran `bin/cake migrations migrate` against the fresh local Postgres — **all 6 migrations applied with zero changes needed**, confirming the phinx files were already portable (DB-agnostic abstract types: `uuid`, `string`, `integer`, `datetime`; no raw MySQL SQL anywhere in `api/src/Controller`/`Service`/`Model`, confirmed by grep before starting). `composer test` (167 tests, PHPUnit on its usual sqlite test connection) unaffected, all green. Booted `bin/cake server`, signed up a real user against local GoTrue, and drove the full stack end-to-end over HTTP: JWT verified against local JWKS → JIT-provisioned a `users` row with native Postgres `uuid` columns and correct quota defaults → created an org, a board, a list, and a card through the real API, all persisted and readable back. This is the proof the migration is real, not just "migrations ran." [Architecture](architecture.md), [Data Model](data-model.md), [Supabase Migration](supabase-migration.md), and [Roadmap](roadmap.md) updated to describe Postgres as the current local-dev state; prod (negrita) still runs MySQL until the deploy cutover.
* **Cutover concern resolved: no data migration.** User decided to nuke whatever's running on negrita and rebuild the deploy from scratch rather than migrate its live MySQL data to Postgres — this removes the export/transform/load/rollback-path work `supabase-migration.md`'s original "Cutover concern" section called out as the thing that made this a project rather than a schema swap. Deploy work itself is deferred until local dev is fully solid; not started this pass.

## 2026-08-30

* **Prod cutover complete — Supabase migration fully done, no MySQL left anywhere.** Ran the one-time schema migration against the `mlowpaysruhaxowhsuva` project's hosted Postgres (all 6 migrations, verified with `bin/cake migrations status` before/after — before showed all `down` on a genuinely empty database, confirming nothing needed carrying over). Removed the orphaned `ollert-api-db` MariaDB accessory container on negrita and its data directory, then redeployed `config/deploy.api.yml` (already rewritten in the post-merge-cleanup branch, PR #36 — no accessory block, `pdo_pgsql` instead of `pdo_mysql`, `DATABASE_URL` pointed at hosted Postgres). Three real gotchas hit and fixed, all logged in `CLAUDE.md`: (1) Supabase's dashboard gives a `postgresql://` scheme, which CakePHP's DSN parser doesn't recognize (only `mysql`/`postgres`/`sqlite`/`sqlserver` map to driver classes) — needs rewriting to `postgres://`; (2) a shell `DATABASE_URL=<x> bin/cake ...` prefix doesn't override `api/.env`'s own value — CakePHP's dotenv loader raises `LogicException` on an already-defined key instead of overwriting it, fixed with `set -a; source api/.env; DATABASE_URL=<x>; set +a`; (3) the bigger one — Supabase's **direct** Postgres connection resolves to an IPv6-only address, and negrita's Docker containers have no IPv6 route out, so the first redeploy attempt failed every DB connection with `Network unreachable` even though the identical string worked from a local shell moments earlier. Switched to Supabase's connection **pooler** (`aws-0-us-west-2.pooler.supabase.com:6543`, IPv4-reachable, transaction mode — which also matches CakePHP's non-persistent connection shape) and redeployed clean. Verified live: `/health` 200, `bin/cake migrations status` inside the actual running container shows all 6 migrations `up`. `ollert-web`/`ollert-mcp` untouched — neither depends on the DB. `planning/architecture.md`, `data-model.md`, `supabase-migration.md`, and `roadmap.md`'s remaining "prod still runs MySQL" / "cutover pending" caveats all updated to reflect completion. `post-merge.md`'s 3 remaining items are now fully checked off.
* **Admin feature (#20) backend + full audit logging implemented** (`feat/admin-audit-logs`), per the design in [permissions.md](permissions.md) and [api-contract.md#admin](api-contract.md#admin): `AddIsAdminToUsers`/`CreateAuditLogs` migrations; `App\Service\AuditLogService` (`diffForCreate`/`diffForUpdate`/`diffForDelete`/`write`, following the existing `QuotaService`/`OrgAuthorizationService` explicit-call style, no ORM-event magic); wired into all ~15 create/update/delete actions across `OrganizationsController`/`OrgMembersController`/`BoardsController`/`ListsController`/`CardsController`; new `AdminUsersController` (`GET`/`PATCH /api/admin/users`) and `AuditLogsController` (`GET /api/admin/audit-logs` platform-wide, `GET /api/orgs/:id/audit-logs` owner-scoped); `ADMIN_UUID` bootstrap in `AuthMiddleware::findOrCreateUser()`'s new-row path. One real bug caught by the test suite: `Cake\ORM\Entity::getDirty()` already returns a list of field names (not an assoc array), so `array_keys($entity->getDirty())` in `diffForUpdate()` was iterating integer indices instead of field names, 500ing every `edit` action across the app — fixed to iterate `getDirty()` directly. A second bug only a live smoke test caught: `AuditLogsTable`'s `buildRules()` had an `existsIn` check on `org_id` against `Organizations`, whose default finder is Trash-scoped (excludes soft-deleted rows) — deleting an org would then fail 422 trying to write that same delete's own audit row, since the org it points to had just been (soft-)deleted. Removed the app-level `existsIn` on `org_id` (the DB-level FK is enough — the row still physically exists). Test isolation gap: `audit_logs` needed its own (empty) fixture (`AuditLogsFixture`) added to every controller test class that now writes real audit rows, or `TruncateStrategy` never learns to clean the table between tests and stale rows collide with the next test's fixture inserts on `users`' unique `supabase_uid` — including a previously-fixture-only-`Users`/`Organizations` regression test (`CsrfApiScopeIntegrationTest`) that actually hits `OrganizationsController::add()` for real. `composer test` (167 tests) and `composer cs-check` both clean. Live-smoke-tested end-to-end against local Postgres (real Supabase JWT, `is_admin` flipped directly in Postgres): admin list/promote/quota-override, both audit-log endpoints with real before/after diffs on org+board create/update/delete, and both 403 gates (non-admin, non-owner) all verified working. Docs updated: [api-contract.md#audit-log](api-contract.md#audit-log) (new section), [data-model.md#audit_logs](data-model.md#audit_logs) (new entity), [roadmap.md](roadmap.md) (Admin #20 marked backend-done). Frontend (admin UI, audit log pages) not yet built — remaining piece of this feature.

## 2026-08-31

* **Prod migrations get a first-class connection: `bun migrate:prod`.** Added a `prod` Datasources entry in `api/config/app.php` mirroring `default` (Postgres, `persistent`, `EMULATE_PREPARES` — same Supabase pooler) but reading `env('DATABASE_URL_PROD', null)`. `scripts/sync-env.sh` (`bun run env`) now cross-feeds the API target: it copies the root `KAMAL__DATABASE_URL` (the prod connection string, already maintained for kamal deploys) into `api/.env` as `DATABASE_URL_PROD`. New root script `bun migrate:prod` = `cd api && bin/cake migrations migrate --connection prod`. Decisions: (1) a *second* connection rather than overriding `DATABASE_URL` in place — keeps `default` pointed at local dev so the rest of the app boots normally, and sidesteps the 2026-08-30 `LogicException`-on-already-defined-key dance entirely; (2) sourced from `KAMAL__DATABASE_URL` so the prod string has one source of truth, not two places to drift; (3) `DATABASE_URL_PROD` is unset in the prod *container* (env there is `.kamal/secrets`-sourced, not the local `api/.env`), so the `prod` connection stays `url => null` there and is never loaded — CakePHP lazy-loads connections, defining it costs nothing at runtime. The manual source-then-override dance (CLAUDE.md Learnings 2026-08-30) is superseded for routine prod migrations; it still works as a one-off escape hatch against an arbitrary DB. Motivation: prod was 2 migrations behind (`AddIsAdminToUsers`/`CreateAuditLogs` down in prod, up locally) and the only ways to apply them were a deploy or the manual dance. Docs: `DEPLOYMENT.md` §3 rewritten around `bun migrate:prod`; `api/.env.example`, root `.env.example`, `README.md`, `api/CakePHPDivergance.md`, `planning/architecture.md` updated.
